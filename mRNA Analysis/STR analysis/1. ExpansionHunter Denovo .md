# STR analysis using ExpansionHunter Denovo
*This step describes how ExpansionHunter Denovo (EDH) can be used to investigate candidate short tandem repeat (STR) regions in aligned.*

## Requirements
ExpansionHunter Denovo must be installed and available.

A reference genome in FASTA format is required. 

Aligned BAM files are required as input. 

## 1. Generate STR profiles
Generate an STR profile for each sample:
```
ExpansionHunterDenovo profile \
 --reads sample.bam \
 --reference reference_genome.fa \
 --output-prefix output/sample_name \
 --min-anchor-mapq 50 \
 --max-irr-mapq 4
```

This creates a JSON profile containing repeat-associated read information for the sample. 

Repeat 
